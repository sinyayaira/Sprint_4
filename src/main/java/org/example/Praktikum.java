package org.example;

public class Praktikum {
    public static void main(String[] args) {
        String cardHolderName = "Name Lastname";
        Account account = new Account(cardHolderName);
        account.checkNameToEmboss();

        /*
             В этом методе заложи логику работы с классом org.example.Account.
             Нужно создать экземпляр класса org.example.Account: в качестве аргумента передать тестируемое имя
             и вызвать метод, который проверяет, можно ли использовать фамилию и имя для печати на банковской карте.
         */
    }
}
